# pi records the changelog version it has shown into its own settings file.
# Git compares the file without that field so the write never becomes a commit.
home/.pi/agent/settings.json filter=pi-runtime-state
